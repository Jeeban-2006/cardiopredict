# Comma-separated list of allowed CORS origins in production.
ALLOWED_ORIGINS=https://cardiopredict-sepia.vercel.app,http://localhost:5173

# Listening port for Gunicorn / Render startup script (automatically assigned by Render in production)
PORT=5000
